
#include "keyboard.h"

namespace {

} // namespace

namespace vscode_keyboard {

std::vector<KeyMapping> GetKeyMapping() {
  std::vector<KeyMapping> result;

  return result;
}

} // namespace vscode_keyboard
